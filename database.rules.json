{
  "rules": {
    "status": {
      "$userId": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $userId",
        "state": {
          ".validate": "newData.isString() && (newData.val() == 'online' || newData.val() == 'offline' || newData.val() == 'idle')"
        },
        "lastChanged": {
          ".validate": "newData.exists()"
        }
      }
    },
    "users": {
      "$userId": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $userId"
      }
    },
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null",
        
        "players": {
          ".read": "auth != null",
          ".indexOn": ["joinedAt", "isOnline"],
          "$playerId": {
            ".write": "auth != null && (auth.uid == $playerId || data.exists())",
            ".validate": "newData.hasChildren(['id', 'name', 'score', 'isReady', 'isOnline'])",
            "id": {
              ".validate": "newData.isString() && newData.val() == $playerId"
            },
            "name": {
              ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 50"
            },
            "score": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            },
            "isReady": {
              ".validate": "newData.isBoolean()"
            },
            "isOnline": {
              ".validate": "newData.isBoolean()"
            }
          }
        },
        
        "gameState": {
          ".read": "auth != null",
          ".write": "auth != null"
        },
        
        "answers": {
          "$questionIndex": {
            "$userId": {
              ".read": "auth != null",
              ".write": false
            }
          }
        },
        
        "leaderboard": {
          "$userId": {
            ".read": "auth != null",
            ".write": false
          }
        },
        
        "submissions": {
          "$userId": {
            ".read": "auth != null && auth.uid === $userId",
            ".write": "auth != null && auth.uid === $userId"
          }
        },
        
        "presence": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid == $userId",
            ".validate": "newData.hasChildren(['isOnline', 'lastSeen', 'username'])",
            "isOnline": {
              ".validate": "newData.isBoolean()"
            },
            "lastSeen": {
              ".validate": "newData.isNumber()"
            },
            "username": {
              ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 50"
            }
          }
        },
        
        "playerStatuses": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid == $userId",
            ".validate": "newData.hasChildren(['isReady', 'updatedAt'])",
            "isReady": {
              ".validate": "newData.isBoolean()"
            },
            "updatedAt": {
              ".validate": "newData.isNumber()"
            }
          }
        },
        
        "game": {
          ".read": "auth != null",
          ".write": "auth != null",
          "timer": {
            ".validate": "newData.hasChildren(['timeLeft', 'isRunning', 'updatedAt'])",
            "timeLeft": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            },
            "isRunning": {
              ".validate": "newData.isBoolean()"
            },
            "updatedAt": {
              ".validate": "newData.isNumber()"
            }
          },
          "currentQuestion": {
            ".validate": "newData.isNumber() && newData.val() >= 0"
          },
          "updatedAt": {
            ".validate": "newData.isNumber()"
          }
        },
        
        "answerProgress": {
          "$userId": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid == $userId",
            ".validate": "newData.hasChildren(['hasAnswered', 'answeredAt'])",
            "hasAnswered": {
              ".validate": "newData.isBoolean()"
            },
            "answeredAt": {
              ".validate": "newData.isNumber()"
            }
          }
        },
        
        "chat": {
          ".read": "auth != null",
          "messages": {
            ".indexOn": ["timestamp"],
            "$messageId": {
              ".write": "auth != null",
              ".validate": "newData.child('userId').val() == auth.uid && newData.child('username').isString() && newData.child('message').isString() && newData.child('timestamp').isNumber()",
              "userId": {
                ".validate": "newData.isString() && newData.val() == auth.uid"
              },
              "username": {
                ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 50"
              },
              "message": {
                ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 500"
              },
              "timestamp": {
                ".validate": "newData.isNumber()"
              },
              "type": {
                ".validate": "!newData.exists() || (newData.isString() && (newData.val() == 'user' || newData.val() == 'system' || newData.val() == 'announcement'))"
              },
              "photoURL": {
                ".validate": "!newData.exists() || newData.isString()"
              }
            },
            "typing": {
              "$userId": {
                ".read": "auth != null",
                ".write": "auth != null && auth.uid == $userId"
              }
            }
          }
        }
      }
    }
  }
}
